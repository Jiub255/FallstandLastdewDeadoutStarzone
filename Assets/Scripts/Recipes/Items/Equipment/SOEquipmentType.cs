using UnityEngine;

[CreateAssetMenu(fileName = "New Equipment Type SO", menuName = "Items/SOEquipmentType")]
public class SOEquipmentType : ScriptableObject
{
	// Just using as an enum, except for SOWeaponType child class, which holds an attack rate float. 
}